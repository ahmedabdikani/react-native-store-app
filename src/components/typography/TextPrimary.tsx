import styled from "styled-components";

import Text from "../theme/Text";
import { tintColorLight } from "../../constants/Colors";

const TextPrimary = styled(Text)({ color: tintColorLight });

export default TextPrimary;
